def generate(name, spec, template_type):
    """
    Name is the deployment name
    spec is the deployment spec
    template_type is the output template format
    return is path to the generated template
    """
    generated_path = ""
    return generated_path
